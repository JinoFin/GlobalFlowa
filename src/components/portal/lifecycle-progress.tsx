import { lifecycleInfo, lifecycleProgress, lifecycleStages, type LifecycleStage } from "@/lib/request-lifecycle";

export function LifecycleProgress({ stage, updatedAt, nextAction }: { stage: LifecycleStage; updatedAt: string | null; nextAction: string }) {
  const current = lifecycleStages.indexOf(stage);
  return <section className="rounded-md border border-navy-100 bg-white p-6 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-teal-700">Request progress</p><h2 className="mt-2 text-2xl font-semibold text-navy-950">{lifecycleInfo[stage].label}</h2><p className="mt-2 text-sm text-navy-650">{lifecycleInfo[stage].description}</p><div className="mt-5 h-2 overflow-hidden rounded-full bg-navy-100"><div className="h-full rounded-full bg-teal-600" style={{ width: `${lifecycleProgress(stage)}%` }} /></div><ol className="mt-5 grid gap-2 sm:grid-cols-3">{lifecycleStages.map((item, index) => <li key={item} className={`rounded-md px-3 py-2 text-xs font-semibold ${index < current ? "bg-teal-50 text-teal-800" : index === current ? "bg-navy-950 text-white" : "bg-navy-50 text-navy-500"}`}>{lifecycleInfo[item].label}</li>)}</ol><div className="mt-5 rounded-md bg-navy-50 p-4"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-teal-700">Next customer action</p><p className="mt-1 text-sm font-semibold text-navy-950">{nextAction}</p></div>{updatedAt ? <p className="mt-3 text-xs text-navy-500">Last updated {new Date(updatedAt).toLocaleString()}</p> : null}</section>;
}
